# DoubleY Portfolio

Composer portfolio website built with React and Vite.

## Development

```bash
pnpm install
pnpm dev
```

## Production build

```bash
pnpm build
```

The build output is generated in `dist/`. Vercel can detect and deploy this Vite project with the default settings.
